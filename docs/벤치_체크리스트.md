# 벤치 체크리스트 — 보드 5기 (인쇄용)

> **한 줄 요약**: 보드 도착 → 서버(LAN) 기동 → 유닛별 5게이트(G0 플래시 → G1 GPS전원 → G2 풀사이클 → G3 버퍼플러시 → G4 HTTPS) → 공통 2항(슬립 전류·조립 이행). **전 게이트 통과 전엔 조립·방류 없음.** 근거: [firmware/README](../firmware/README.md) 벤치 재검증 체크리스트 · [조립_가이드](조립_가이드.md).

## 0. 사전 준비 (보드 도착 전에 완료)

| ✅ | 항목 |
|---|---|
| ☐ | Arduino IDE + ESP32 core 3.0.x, 보드 "ESP32 Dev Module" |
| ☐ | 라이브러리: **lewisxhe/TinyGSM-fork**(★스톡 TinyGSM 제거), TinyGPSPlus, ArduinoHttpClient |
| ☐ | 수신 서버 v1.1 확인: `python ingest_server.py 8770 0.0.0.0` — 기동 로그에 `FF-ID schema v1.1` |
| ☐ | ★★**서버를 공인 인터넷에 노출** — 보드는 Wi-Fi가 아니라 **LTE(통신사 망)**로 접속하므로 **LAN 사설 IP(192.168.x.x)로는 절대 도달 불가**(통신사 NAT). 둘 중 하나: ①`ngrok tcp 8770` → 발급된 `호스트:포트`를 펌웨어 `SERVER_HOST/PORT`에 ②공유기 포트포워딩(8770)+공인 IP. ※`ngrok http`는 TLS라 평문 클라이언트로 안 붙음(펌웨어 주석 참조) |
| ☐ | 방화벽 8770 인바운드 허용(포트포워딩 경로일 때) |
| ☐ | (서버 드라이런) curl로 v1.1 ping POST → `ok:true`, 같은 seq 재전송 → `dup:true`, `/api/live.geojson`에 궤적 |
| ☐ | SIM 5장(Soracom/1NCE 또는 KT·SKT 데이터심 — ★LG U+ 불가), 보호 18650 5개(연속 3A+) 충전 |
| ☐ | 유닛 ID 라벨 5장 인쇄: `ff-kr-bs-u0001` ~ `u0005` |

## 1. 유닛별 게이트 (보드마다 반복)

- **G0 플래시**: `DEVICE_ID`를 유닛 고유값으로 바꿔 업로드(`BENCH_HTTP 1`, `SLEEP_MINUTES 5`). ★18650 장착 후 진행(USB 단독 급전 금지 — 2A 피크 브라운아웃).
- **G1 GPS 전원 관문**: 창가/야외에서 첫 웨이크 로그 확인. fix 성공 또는 실패라도 `NMEA chars>0`이면 통과. **`chars=0`이면 중단** → L76K가 모뎀 전원에 의존 → [firmware/README](../firmware/README.md) 체크리스트 1번 조치(modemPowerOn을 getFix 앞으로) 후 전 유닛 재플래시.
- **G2 풀사이클**: fix → POST 200 → 딥슬립 → 웨이크, 3주기 연속. 서버에서 `seq`가 1씩 증가, `ts_fix`·`sample_interval_s`·`batt` 수신 확인.
- **G3 버퍼 플러시**: LTE 안테나 분리 → 2~3주기(로그에 "버퍼 보존" 확인) → 안테나 재결합 → 다음 웨이크에 **밀린 레코드가 오래된 순으로 몰아 전송**되고 지도 궤적이 ts_fix 순으로 조립되는지.
- **G4 HTTPS 빌드**(대표 1기만): `BENCH_HTTP 0`(A76XXSSL)로 컴파일·업로드 → openc로 POST 성공. 확인 후 벤치용은 다시 `BENCH_HTTP 1`.

| 유닛 | G0 플래시 | G1 GPS전원(chars) | G2 풀사이클(seq) | G3 버퍼플러시 | 비고 |
|---|---|---|---|---|---|
| ff-kr-bs-u0001 | ☐ | ☐ | ☐ | ☐ | G4 HTTPS 대표 ☐ |
| ff-kr-bs-u0002 | ☐ | ☐ | ☐ | ☐ | |
| ff-kr-bs-u0003 | ☐ | ☐ | ☐ | ☐ | |
| ff-kr-bs-u0004 | ☐ | ☐ | ☐ | ☐ | |
| ff-kr-bs-u0005 | ☐ | ☐ | ☐ | ☐ | |

## 2. 공통 항목 (1회)

| ✅ | 항목 |
|---|---|
| ☐ | **슬립 전류 실측**(가능 장비 시): USB 전류계/멀티미터로 딥슬립 전류 기록 → [ELECTRONICS_POWER](../hardware/ELECTRONICS_POWER.md) 추정치를 실측으로 교체 |
| ☐ | 배터리 전압 vs `batt` 보고값 대조(분압 계수 보정) |
| ☐ | 벤치 로그 보관(유닛별 시리얼 캡처) — 유닛 메타디렉토리 `verified_build` 근거 |

## 3. 다음 단계

전 유닛 G0~G3 통과 → [조립_가이드](조립_가이드.md) 3장(조립)으로. 조립·밀봉 후 T1~T6([VERIFIED_BUILD_TEST](../hardware/VERIFIED_BUILD_TEST.md)) → 방류 전 [PRELAUNCH_RECOVERY_CHECKLIST](../hardware/PRELAUNCH_RECOVERY_CHECKLIST.md).

## 4. 솔라 부록 — G5 수확 (Tier 1.5 솔라 변형만)

솔라 유닛은 위 G0~G3 + 조립·T1~T6에 더해 **G5(수확 특성화)·T7(셀 장착 자기복원)** 를 통과해야 방류. G5 7스텝(인라인 전류계/쿨롱카운터, 최소 USB 전류계로 대체 가능):

| ✅ | 단계 |
|---|---|
| ☐ | ① 완전 햇빛·정적 최적 자세 기준 충전전류 |
| ☐ | ② **뒹구는 자세** 시간평균 전류(자기복원체는 고정면 없음·배는 항상 어두움) |
| ☐ | ③ 모의 탁도/수막 얹고 재측정 |
| ☐ | ④ 노화 반영(a-Si는 초기 10~30% 안정화 후) |
| ☐ | ⑤ 일주 적분 → 수확 H(mAh/day) |
| ☐ | ⑥ **야간 컨트롤러 정지전류 실측** — mA급 MPPT면 순손해 → 선형/야간게이트 |
| ☐ | ⑦ 목표 핑 실측 소비 D와 비교, **손익분기 H*≈132 mAh/day**(60분·모뎀오프). 연장계수 1/(1−H/D)만, ★**일수 인용 금지** |

**판정**: H가 D 대비 순증이 아니면 "보험 트리클"로 라벨(배터리가 잔류를 독립 감당). <0℃면 저온 컷오프로 H→0(겨울은 솔라 아님). 상세=[TIER1_5 §4](../hardware/TIER1_5_SOLAR_DRIFTER.md).

---
> 정직 각주: 모든 전력·부력 수치는 추정이며 이 벤치가 실측으로 교체한다. 셀룰러(위성 아님)·회수-우선. 다수 방류 전 전파 면제확인 필요.
